# 2026-08-04

- 修复 Windows 上 Bun 1.3.14 执行 `bun install` 时的 `IntegrityCheckFailed` 与嵌套依赖 `ENOENT`。根因是 Bun 全局包缓存/部分提取的 `node_modules` 状态异常，并伴随从 `pnpm-lock.yaml` 的首次迁移。
- 执行 `bun pm cache rm`，随后以 `bun install --force --no-cache --backend=copyfile --registry=https://registry.npmjs.org` 重装，成功安装 381 个包并生成被仓库忽略的本地 `bun.lock`。
- 验证结果：`bun install --frozen-lockfile` 无变更通过；`bun run build` 构建成功；`bun run test` 的 9 个测试文件、29 个测试全部通过。
- 仓库仍以 `packageManager: pnpm@11.10.0` 和 `pnpm-lock.yaml` 为正式包管理配置；如继续使用 Bun，本机保留 `bun.lock` 可避免每次重新迁移。
- 修复 Bun 开发启动：将 `package.json` 的 `dev` 和 `src-tauri/tauri.conf.json` 的 `beforeDevCommand` 从 `pnpm exec vite` 改为直接调用本地 `vite`。`bun run dev` 已验证成功。Tauri CLI 已能进入启动流程，但本机缺少 `cargo`/`rustc`，需安装 Rust MSVC 工具链后才能运行桌面端。
- 进一步核查本机 Tauri 前置环境：Rustup/Rust/Cargo 未安装，Visual Studio 2022 Build Tools 及 MSVC C++ x64/x86 组件也未检测到；需安装两者后重启终端。
- 完成 Tauri 调试链路修复：确认 Rust 1.97.1、Cargo 1.97.1、stable MSVC 工具链与 Visual Studio C++ 工具可用；Tauri 包装脚本会自动把 `%USERPROFILE%\.cargo\bin` 注入子进程 PATH。
- 恢复 Git LFS 管理但工作树缺失的 `src-tauri/bin/ffmpeg-x86_64-pc-windows-msvc.exe`（144467968 字节），并验证其 MZ/PE/x64 头及 `ffmpeg -version` 均正常。
- 生成 `src-tauri/resources/python-runtime` 以满足 Tauri 资源 glob；同步更新官方 `get-pip.py` 的固定 SHA-256 为 `25b5c39ade96bab5eabe6404ce83cab6da2deb5fe3c07d9881f43803edb6f9c8`，嵌入式 Python 与依赖安装验证通过。
- 最终验证 `bun run tauri dev`：Rust dev profile 编译成功，启动 `.cargo-target/debug/VrcDog.exe`；VRCDog 窗口存在且响应正常，Vite 在 `127.0.0.1:1420` 监听并返回 HTTP 200。验证后已停止调试任务，VRCDog 进程和 1420 端口均已清理。
- 修复 VRChat 2FA 邮箱验证码链路：统一规范化 `emailOtp`/`totp`/`otp`，多方式时默认邮箱并提供方式切换；验证码会去除复制空白并校验 6 位数字；登录临时 Cookie 贯穿验证请求和最终 `/auth/user`，验证响应 Cookie 会合并保存，服务端失败信息不再被统一覆盖。
- 新增 `twoFactor` 纯函数、AuthApi 路由和 LoginView 组件回归测试；最终 `bun run test` 为 12 个文件、43 个测试全部通过，`bun run build` 成功。
- 定位并修复 Midishow 登录 403：当前登录页受 Cloudflare 浏览器验证保护，Rust/Python 直接提交密码会被风控拒绝。新增 Tauri 内置 Midishow 登录 WebView、从 WebView2 读取 HTTP-only Cookie 并验证后保存的命令；前端改为“打开内置浏览器登录 → 导入已登录会话”，手动 Cookie 作为备用，禁用密码直登和密码回退，账户仅展示经过验证的浏览器 Cookie 会话。
- 完成 Midishow 修复收尾：历史 password-only 账号不会阻挡有效 Cookie 账号；下载流程不再调用旧 Python 密码回退；清理未使用的密码登录/Python 桥接函数。最终验证：vue-tsc 通过，Vite 生产构建通过，Vitest 12 个文件/43 个测试通过，Rust cargo check 通过。OpenVR CMake 在默认并行构建时会出现 MSB4166，清理缓存后用 NMake 串行检查可稳定通过。
- 完成 VRPiano 与 Bilibili 弹幕体验改造：当前曲目长文本安全截断；Midishow 改为账号密码触发内置浏览器自动填表、轮询真实用户名并采用一次性密码策略；调试事件递归脱敏，登录成功后关闭 WebView；Bilibili 仅在 AUTH_REPLY code=0 后标记连接成功，鉴权失败进入友好退避重连；桌面和 OpenVR 面板改为深色粉色直播姬风格，并修复 VR 背景透明度范围。
- 验证收尾：LoginView 单测 4/4 通过，完整 Vitest 12 个文件/43 个测试通过；vue-tsc、Vite production build、cargo fmt --check 均通过。Git 索引当前异常地将全部跟踪文件暂存为删除、工作树同名文件显示未跟踪；未执行 reset/clean/提交等可能覆盖用户工作的操作，需后续单独确认后再修复索引。
- 完成本轮最终收尾：Midishow 新增不携带凭据的页面刷新后错误/人工确认监测，用户界面统一为“登录会话”；OpenVR CMake 强制 Release 与 MultiThreadedDLL，Rust 测试成功链接并通过 23/23。前端 vue-tsc、Vite 构建和 VM 隔离池下 Vitest 12 文件/43 测试全部通过。
- 已选择性提交 7 个目标文件，提交为 `22b3640 feat: improve VRPiano login and live assistant`，未纳入 OpenVR 二进制、生成物及其他既有修改。完成 GitHub 浏览器授权后已成功推送，`origin/main` 与本地均为 `22b364059efd3ce9fa7b591fef3d63719ceeb5f7`。
